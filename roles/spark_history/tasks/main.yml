---
# file: roles/spark/tasks/main.yml

- name: copy init file
  template: src=spark-history.sh dest=/etc/init.d/spark-history.sh owner=root group=root mode=755
  tags:
    - spark
    - start-spark-history

- name: create log direcory
  file: path={{spark.eventLog}} state=directory mode=777
  tags:
      - spark
      - start-spark-history

- name: start spark history
  service: name=spark-history state=restarted
  tags:
    - spark
    - start-spark-history

